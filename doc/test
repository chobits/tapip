+ TOP1:
================================================
1. test icmp and ip fragment/defragment:
 # sudo ./net_stack
 # ping 10.20.133.20
    (ping gateway, and see debug informantion)
 # ping -s 60000 10.20.133.2
    (ping some user via 60000-bytes icmp data)




+ TOP2:
(bob 10.0.0.1 ./net_stack)
(tom 10.0.0.2  tap0)
================================================
1. test arp:

(bob)	bob # sudo ./net_stack
(bob)	[net shell]: debug arp
	(waiting debug information)

(tom)	tom # sudo route add -net 10.0.0.0 netmask 255.255.255 tap0
	tom # sudo arping 10.0.0.1 -I tap0

================================================
2. test ip(tap0 -> ./net_stack):

(tom)	tom # sudo tcpdump -vvv -i eth0
	(waiting debug information)

(bob)	bob # sudo ./net_stack
(bob)	[net shell]: ping -s 6000 10.0.0.2


================================================
3. test ip(./net_stack -> tap0):

(tom)	tom # ping 10.0.0.1

(bob)	bob # sudo ./net_stack
(bob)	[net shell]: ping -s 60000 10.0.0.2
	(see debug information)


================================================
4. test ip forward:

(bob)	bob # sudo route add -net 10.0.1.0 netmask 255.255.255.0 gw 10.0.0.1
	bob # sudo ./net_stack
	[net shell]: debug ip
	(waiting debug information)

(tom)	tom # ping 10.0.1.1

================================================
5. test:
	1.icmp redirect
	2.icmp time exceeded(ttl)

(bob)	bob # sudo route add -host 10.0.0.3 gw 10.0.0.1 tap0
	bob # sudo ./net_stack
	[net shell]: debug ip icmp
	(waiting debug information)

(tom)	tom # ping 10.0.0.3 -c 1
	(waiting redirect reply)
	tom # ping 10.0.0.3 -c 1 -t 1
	(waiting time exceeded reply)

================================================
6. test icmp defragment timeout

(bob)	bob # (write #define ICMP_EXC_FRAGTIME_TEST into include/netcfg.h)
	bob # sudo ./net_stack
	[net shell]: debug ip icmp
	(waiting debug information)

(tom)	tom # ping 10.0.0.1 -c 5 -s 60000
	(waiting frag reassembly time exceeded reply)

================================================
7. test icmp fragment needed

